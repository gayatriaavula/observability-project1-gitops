# DEMO ONLY. Never commit real secret values to git.
#
# In a real deployment, replace this plain Secret with either:
#   - External Secrets Operator, syncing from AWS Secrets Manager, or
#   - Bitnami Sealed Secrets, so only an encrypted blob is committed.
# Argo CD would then sync the *reference*, not the plaintext value.
apiVersion: v1
kind: Secret
metadata:
  name: orders-backend-secret
  namespace: orders-backend
type: Opaque
stringData:
  DEMO_API_KEY: "placeholder-value-replace-me"
